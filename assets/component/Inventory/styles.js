import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  value: {
    fontWeight: "600",
    fontSize: 20,
    textAlign: "center",
  },
  header: {
    fontWeight: "300",
    fontSize: 12,
    textAlign: "center",
    color: "grey",
  },
  textContainer: {
    width: "90%",
  },
});

export default styles;
